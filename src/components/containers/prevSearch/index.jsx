import { GifContainer } from '@gif/gifList';
import './prevSearch.css'
export function PrevSearch() {
  return (
    <section className="PrevSearch">
      <div className="container">
        <h2 className='PrevSearch__title'>Previous search</h2>

        {/* <ul className="SearchedGrid">
          <GifContainer gifs={gifs} list={true} loading={loading}/>
        </ul> */}
      </div>
    </section>
  )
} 